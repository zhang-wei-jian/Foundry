// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.13;

contract Counter {
    uint256 public number;

    function setNumber(uint256 newNumber) public {
        number = newNumber;
    }

    function increment() public {
        number++;
    }

    //   function sort() public {
    //     uint A = 80;// A (80 ETH)
    //     uint B = 100;// B (100 ETH)
    //     uint C = 60;// C (60 ETH)
        
    //      address Max1 ;// A (80 ETH)
    //     address Max2;// B (100 ETH)
    //     address Max3;// C (60 ETH)
        
    //     if(A>=B && A>=C){//A是最大的
    //     Max1 =A;
    //     }else if(B>=A && B>=C){//b是最大的

    //     }else{//c最大

    //     }



    // }
    //   function sort() public {
    //     uint A = 80;// A (80 ETH)
    //     uint B = 100;// B (100 ETH)
    //     uint C = 60;// C (60 ETH)
        
    // // for (uint i = 0; i < array.length; i++) {
        
    // // }



    // }
}
